"""Port interfaces for the adapter pattern.

Services depend on these ABCs, never on concrete implementations.
Adapters in app.adapters.supabase/ provide the concrete implementations.
AWS adapters were retired during the v2 migration.
"""
